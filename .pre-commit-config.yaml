repos:
  - repo: local
    hooks:
      - id: quality
        name: Qualidade da etapa (ruff, mypy, pytest)
        entry: make check
        language: system
        pass_filenames: false
        always_run: true
